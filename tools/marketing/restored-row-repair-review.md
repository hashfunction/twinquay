# Restored receipt capture repair — 2026-09-12

The failure in native capture `34685566093` was a capture assertion about Qt's accessibility provider. The original installed consumer workflow completed all seven operations and nine proof surfaces, including persistent collision recovery and restored-file byte verification. `workflow/workflow.json` reports `passed: true`, process `3620`, source `73f3842a2cc81aa3c69a5873a262517eae35b6f4`, and no workflow errors. The later marketing assertion failed before its third image and normal close. That run is not a completed capture: `captured` and `normal_close_verified` are false; cleanup stopped the retained process with exit `-1`, left no package registrations and reported no cleanup errors. Its original unsigned package stayed unchanged.

The downloaded `restored-receipt-observation.json` and native PNG were read and viewed before changing code. The checked-in JSON fixture preserves that actual observation. It identifies the visible `Quarantine Receipts — DupliSift` window, PID `3620`, HWND/root `590456`, native class `Qt6112QWindowIcon`. Its five DataItems expose the exact original path `C:\Demo\Cedar House Review\Project Documents\Cedar House Brief - emailed.txt` and status `restored`. Every DataItem reports UIA `IsEnabled=true`, including the visibly grey, unchecked restore cell. The original file is 15,335 bytes, SHA-256 `e0ae42ac283fb94f96458d82486515856c2c8d32381e14860b6c8a008edc3da3`. The PNG hash recorded by the actual observation is `01b1eede870b6be2cdf3647b616cebea3647c32c2c27303fd621f9d1228f0b9e`.

The exact Qt source explains the value. In `qtbase/src/widgets/accessible/itemviews.cpp:1257`, `QAccessibleTableCell::state()` starts with an empty state and derives checked/checkable/selection properties, but never derives `disabled` from `Qt::ItemIsEnabled`. In `qtbase/src/plugins/platforms/windows/uiautomation/qwindowsuiamainprovider.cpp:629`, `UIA_IsEnabledPropertyId` returns `!accessible->state().disabled`. These files were read from the already collected [Qt 6.11.2 original source](https://download.qt.io/archive/qt/6.11/6.11.2/submodules/qtbase-everywhere-src-6.11.2.tar.xz), rehashed locally at 50,582,668 bytes and SHA-256 `5b2e00eccaf5a4d8c14134ffa0ea8dfd0a35ae1ffc7f8d87fa4305a1ed23cf22`, matching the existing immutable source manifest. UIA enabled is therefore not proof of restore eligibility. The unchanged `qt/quarantine_dialog.py` assigns `NoItemFlags` when status is `restored`.

The capture now retains and requires the observed provider value, exact table/DataItem roles, single five-column row, original path, restored status, owned PIDs and visible path/status cells. It writes `restored-table.json` exclusively before asserting the row. Partial property failures retain null fields and a bounded 2,048-character error; cell text is capped at 4,096 characters. The existing bounded full tree and native observation image still precede table lookup.

The same real tree contains two visible enabled Buttons named `Close`: the native titlebar and the Qt dialog button. The additional capture view now dismisses its exact owned QDialog through one ordinary Escape using the original `Send-TwinQuayWorkflowKeys` ownership/foreground guard. It waits at most five seconds for the receipt title and original HWND to disappear from the original owned-window enumeration. A renamed, replaced, duplicate or changed-owner receipt is refused. It then requires the same original main-window HWND, PID and title to return and calls the original empty-key foreground verification before continuing. There is no heuristic Close selection, coordinate input or repeated dismissal input.

Focused verification passed locally using PowerShell 7.6.6 and the existing `env-qt6` Python/PyQt environment:

```text
TMPDIR=/private/tmp ../../filequay/source/.tools/powershell-7.6.6/pwsh -NoProfile -File tools/marketing/test_capture_receipt.ps1
TMPDIR=/private/tmp ../../filequay/source/.tools/powershell-7.6.6/pwsh -NoProfile -File tools/marketing/test_capture_adapter.ps1 -Python "$PWD/env-qt6/bin/python"
TMPDIR=/private/tmp ../../filequay/source/.tools/powershell-7.6.6/pwsh -NoProfile -File tools/marketing/test_capture_helpers.ps1
TMPDIR=/private/tmp ../../filequay/source/.tools/powershell-7.6.6/pwsh -NoProfile -File tools/marketing/test_display_modes.ps1
TMPDIR=/private/tmp ../../filequay/source/.tools/powershell-7.6.6/pwsh -NoProfile -File tools/marketing/test_capture_loader.ps1
env-qt6/bin/python -m unittest discover -s tools/marketing -p 'test_*.py' -v
env-qt6/bin/python tools/marketing/verify_restored_view.py
```

The five PowerShell suites include native-fixture row replay and changed-field refusals, real pre-assert JSON persistence/exclusive-write refusal, partial/oversized provider observations, one Escape followed by exact disappearance/main return, and 14 dismissal refusal cases. The existing original loader/closure, workflow adapter/oracle, frame ownership, cleanup and display tests still pass. All seven Python capture-input tests pass. The actual Qt 6.11.2 replay runs the original quarantine/collision/restore domain flow. It verifies that ordinary click and the original Ctrl+Home/Space route toggle an eligible item, leave the restored item unchecked and uncheckable, and that Escape hides the actual dialog without changing verified restored bytes. These local tests do not claim Windows UIA acceptance.

Only marketing capture helpers, fixtures, documentation and the marketing workflow test list change. Product code, original qualification helpers, package/run/source bindings, original input files, workflow stages, collision/restore byte oracles, signing, normal-close/uninstall and marker-owned cleanup gates remain unchanged. A fresh native capture of the same already-qualified package must still produce all three images, verify normal zero-exit close/uninstall and complete owned cleanup before its screenshots are accepted.
